.onLoad <- function (libname, pkgname)
{

  packageStartupMessage("\nTo pull HIV estimates, use return_type == 'HIV Estimates';
To pull Test & Treat, use return_type == 'Test & Treat'\n")


  invisible ()
}
