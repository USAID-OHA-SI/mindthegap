.onLoad <- function (libname, pkgname)
{

  packageStartupMessage("\nTo pull UNAIDS 2021 estimates from 1990-2020, use munge_aids(). For HIV estimates, use return_type == 'HIV Estimates';
To pull Test & Treat, use return_type == 'Test & Treat'\n")


  invisible ()
}
